module Q {
    
    export class RichFlipFactory extends Flip {

        constructor(
            $el: JQuery,
            options: Options
        ) {
            super($el, options);
        }

    }

}
